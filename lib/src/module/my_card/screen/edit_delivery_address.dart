import 'package:flutter/material.dart';

class EditDeliveryAddress extends StatelessWidget {
  const EditDeliveryAddress({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Edit Delivery Address'),
      ),
    );
  }
}
