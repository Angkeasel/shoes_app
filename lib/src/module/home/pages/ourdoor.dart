import 'package:flutter/material.dart';

class OurDoorPage extends StatelessWidget {
  const OurDoorPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        elevation: 0,
        title: const Text("Ourdoor"),
      ),
      body: Container(),
    );
  }
}
